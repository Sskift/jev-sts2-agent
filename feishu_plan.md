# Slay the Spire 2 Agent Loop：当前执行计划

更新：2026-09-20。目标是 **Jev 在同一局标准游戏中，从第一幕开始，经过第二幕和第三幕，击败最终 Boss，进入正式胜利结算**。目标仍未完成。工作重点是上下文质量、完整回合策略与多步选择的一致性；以实际游戏结果检验，功能优先，只做必要检查。

## 当前运行

第二十六局 `run-1789906322` 在第一幕第 17 层 Vantom 正式失败（分数 171，Boss 剩余 85 生命）。结算、完整记忆已归档并正常返回菜单。运行目录 `run-artifacts/2026-09-20T12-11-59-152Z-f7a98087`，该局加载代码 `f8f6c44`。三幕完整通关仍未完成。

回合末追加动作的完整比较已实机选择继续 Battle Trance，相关历史筛选保持有效。本轮再补齐已核实的易伤/虚弱顺序依赖：用条件范围表示后续伤害，隐藏因新状态而过期的旧点估计。35 项相关检查通过；第 15 层旧局面只读回放改选 Uppercut → Strike，尚无新实战或胜率结论。第二十七局 `run-1789907946` 已加载 `2f455c3`，从标准新游戏第 1 层开始，当前完成前两场战斗并推进到第 4 层、80/80。运行目录 `run-artifacts/2026-09-20T12-39-01-510Z-f0188a1d`，实际 v2 请求已包含新的状态依赖分析。

最新飞书方案已同步到修订 89 并回读确认，包含第二十六局失败、本轮功能及第二十七局开始。历次失败、修复、回放和运行阶段集中保存在[实测进展](docs/full-run-progress.md)及其证据文件中，避免旧阶段被误认为当前状态。

## 已实现的决策管线

1. **取得实时事实。** Node.js 通过本机命名管道调用游戏内 C# 模组。当前生命、能量、格挡、各牌堆、实际牌面、药水、遗物、能力计数、敌人可见意图、地图和合法动作由原生状态提供。游戏自行结算和渲染，正常循环不需要前台或置顶。
2. **补齐相关规则。** 本地保存 Spire Codex v0.111.0 规则快照，按类别、实体 ID 和关联关系取出本次需要的卡牌、遗物、药水、事件、能力和关键词。实时解析值优先于静态基础数值，缺失资料显式标注。
3. **按决策需要选择上下文。** 内部 `sts2.decision.v1` 统一编译为模型输入 `sts2.context.v2`，区分观察、规则、历史、意图、分析和未知。当前回合与上一轮敌方响应默认保留；只有累计、延迟或牌序依赖才带更早记录。旧房间账目、重复快照和过期战略讨论留在本地。独立请求自包含，不假定 Jev 有隐式会话记忆。
4. **保留不同期限的意图。** 整局构筑方向、当前回合目标与有序出牌、一次交互中的具体选牌对象分别更新。营火先选条件升级目标，再比较具体升级与实际休息；确认 Smith 后才将目标交给选牌界面。意图完成即清除，状态或对象不匹配时重新判断。
5. **比较完整方案。** Jev 选择回合目标、主要收益动作、准备动作及其受益对象，再比较有序方案。能量预算与已知出牌限制由代码检查。结束前提议的追加动作也与立即结束作完整比较。可见风险达到门槛时，另问生存约束；没有支持的生存差别则按整体收益选择，不把普通减伤自动置于所有收益之前。
6. **明确计算范围。** 预览和条件算术不冒充完整模拟。已覆盖的触发生成牌以独立条件账目表示，不改写当前牌堆、不预测随机插入和未来手牌。药水提供持续时间、影响对象和先后关系；朝向变化使旧敌方伤害预览失效。已知但未计算的反应记录来源、时机及条件暴露；受其影响的最终 HP、格挡和剩余攻击保持未知。未知抽牌和未来随机结果不按确定收益处理。
7. **执行与复核。** 每个命令执行前后重新读取状态；状态变化则丢弃旧选择。普通连续动作沿用计划，抽牌、目标变化、选择界面等触发复核。结果不明的命令保留 pending，不自动重放。实际请求、答案、命令、原生观察和完整记忆均归档在本地。

架构详见[统一上下文架构](docs/context-architecture.md)。所有模型阶段共用同一编译、容量检查与来源约定。结构完整和格式有效只证明数据通道，不保证模型判断正确。

## 技术与工作约束

- 主路径使用 Node.js 和游戏原生 C# 模组，无 Python 常驻服务、无向量数据库、无必须保持前台的窗口驱动。截图与 Computer Use 仅作可选观察补充。
- 当前原生模组为 `0.111.0-context.17`，面向游戏 v0.111.0，含已验证的兼容修复与额外可见上下文；不将 DLL 或游戏反编译源码提交到本仓库。
- Jev 使用 OpenRouter 原生 Decisions API，配置模型 `typesafe/jev-1.13`。凭据保存在被忽略的本地配置，不写入模型状态、日志或提交。
- 本项目直接在 `master` 开发、commit、push。只有 CLI 模组上游问题才向其仓库提交 PR；已有[上游 Draft PR #1](https://github.com/longkerdandy/STS2-Cli-Mod/pull/1)。
- 不窥探未公开随机结果或抽牌顺序，不编辑存档，不重置失败战斗，不用人工策略命令替代 Jev。
- 用户要求的第一幕 Boss 录像发送已完成：第十九局 Vantom 击杀附近最后约 30 秒已发至本人 liushiao 飞书会话，并经本机 OpenLark 3333 确认。不重新触发已完成的发送任务。

## 接下来的工作与验收

从正常新局继续真实运行，通过路线、奖励、事件、完整回合和多步选择推进三幕。出现问题时，依据实际输入、模型答案、执行结果区分事实缺失、估算错误、意图丢失与模型策略失误，再修复相应环节；不以补几句卡牌提示替代整体信息流设计。

- [x] 完整上下文和回合决策管线接入主循环，普通奖励后继续推进。
- [x] 在实际游戏中验证同一回合计划复用、升级目标衔接及已知出牌限制。
- [ ] 同一 run ID 从第 1 层开始，观察到第一、二、三幕的完整推进。
- [ ] 击败最终 Boss，原生状态为正式胜利 `is_victory=true`，结算可返回菜单；保留完整原生证据。
- [ ] 保存并展示最终胜利截图，更新[现有飞书方案](https://icnainlav1b8.feishu.cn/docx/Ijr1dLJpio6JvNxfcAfcnvXKnUR)的最终结果和重要限制。
- [ ] 将最终实现、计划和证据在主分支提交并推送，然后才结束 goal。

## 运行与资料

- `npm start -- --max-steps 3000`：持续执行当前存档或正常新局。
- `node --use-system-ca scripts/run_recorded.mjs <ffmpeg.exe>`：运行主循环，沿用录像清单；已完成的录制任务不重复启动。详见[录制说明](docs/boss-recording.md)。
- `npm run context:preview`：只读查看当前 JSON，不调用 Jev、不发送游戏动作、不修改记忆。
- `npm test`：离线检查；实际开发按改动运行必要的相关检查。
- [决策 JSON 协议](docs/decision-context.md)、[内部 Schema](schemas/decision-context.v1.schema.json)、[模型 Schema](schemas/model-context.v2.schema.json)、[合成示例](docs/examples/decision-context.v1.json)。
- [技术选型](docs/technology-selection.md)、[早期单场运行记录](docs/validation-2026-09-20.md)。早期记录只证明当时的单场后台运行。

仓库：[Sskift/jev-sts2-agent](https://github.com/Sskift/jev-sts2-agent)。原始运行日志和记忆在本机 `run-artifacts/`，凭据、存档和编译 DLL 不进入版本库。
