# Slay the Spire 2 Agent Loop：当前执行计划

更新：2026-09-20。目标是 **Jev 在同一局标准游戏中，从第一幕开始，经过第二幕和第三幕，击败最终 Boss，进入正式胜利结算**。当前尚未完成这一目标。按最新 goal，近期重点转为改善 Jev 的上下文组织、决策顺序和一致性，再用真实游戏检验通关成功率。

## 当前实现

当前改进转为整条信息流的架构整理：[统一上下文架构](docs/context-architecture.md)已落地。所有 Jev 请求在发送前经过同一个编译器，由内部 v1 数据生成 `sts2.context.v2`：观察、规则知识、历史、意图和分析分区；按实体 ID 关联规则，保留全部原始内容与候选，校验往返一致性，并按编译后的真实大小组织多题批次。v2 已在第二十二局实战接通；该局最终在第一幕 The Kin Boss 失败，不能据此声称决策质量提升。现新增跨房间战略记忆：Jev 评估当前构筑的七类能力、选择发展重点与已有核心；在构筑、幕或房间边界变化时复核，普通出牌复用。判断、发展意图和修订历史分区保存，当前事实可覆盖旧判断。

本地已保存 Spire Codex v0.111.0 英文规则快照，按实体 ID 与关联关系注入每次独立请求。当前状态、历史事实、静态规则和条件估算分别标明来源。

最近补齐 JSON 协议的关系约束：路线统计必须与原始图一致，各规划阶段都有逐步能量预算；实际 139 份请求核查通过。第二十一局第 15 层还修复了已有 Plating 规则与结束格挡估算不一致的问题，立即格挡和结束触发分别表示。该局已击败第一幕 Ceremonial Beast，战后 16/80，并正常进入第二幕、恢复至 80/80，开始第 19 层 Thieving Hopper 战。仍未完成三幕通关。

目前同一局已继续至第二幕第 23 层。新增药水持续时间、作用对象和先后关系的结构化事实，并把未计入药水等效果的方案数字明确放在有限算术基线中，避免遗漏效果被误读成零收益。154 项检查通过；Boss 首回合只读回放开始使用持久力量/敏捷，但仍有多余防御，继续通过当前实战检验。

该局随后在第二幕第 33 层 Crusher + Rocket Boss 正式失败，分数 645，已正常结算。发送时优先在既定容量内展开当前手牌、敌人等关键记录，完整历史不裁剪；实际样本只增加 350 输入 tokens，仍成功调用。复盘补齐 Surrounded 当前朝向、敌人方位和换向后的预览失效关系：不能把旧意图当作换向后的准确伤害。第三幕和正式胜利仍未完成，继续新局验证。

- 主路径是 Node.js 编排 → 本机命名管道 → 游戏内 C# 模组。Jev 负责策略选择，模组执行原生动作，游戏自行结算和渲染。
- 内部采用 `sts2.decision.v1`，每次模型请求由统一编译器生成自包含的 `sts2.context.v2` JSON：本局与玩家资源、永久卡组、战斗各牌堆、敌人可见意图与能力、地图及后续路线、当前交互、相关历史和全部合法选项。观察与条件分析分区，不假定 Jev 记住上一次调用。
- 本地保存各局记忆和动作结果；每次执行前后重读状态。状态变化时重新决策，结果不明的动作保留待核对状态，不自动重放。
- 已接入地图、战斗、药水、事件、奖励与跳过、商店购买与删牌、休息、升级、附魔、遗物与多种选牌界面及幕间过渡。罕见机制随实测补齐。
- 默认无 Python、无额外常驻窗口驱动。截图和 Computer Use 是可选补充，正常循环不依赖前台或置顶。

当前部署模组为 `0.111.0-context.17`，针对本机游戏 v0.111.0 编译，包含 Cauldron 购买奖励通道修复、CalculatedBlock 原生预览读取及 Surrounded 可见朝向与敌人方位。以功能与实际运行优先，只做必要检查。战斗增加回合目标、准备/收益依赖、有序行动、具体升级对象、条件变化后复核和成对完整序列比较，同一轮独立比较合并请求；现在也能比较用一张未用牌替换两张已规划牌。普通连续出牌沿用同一计划；不再默认每张牌独立决定。已经完成过第一幕并到达第二幕 Boss；整局结果和具体问题见[实测进展](docs/full-run-progress.md)。测试通过不等于已经通关。

## 正在执行

当前重点是[统一上下文架构](docs/context-architecture.md)：所有 Jev 请求经过同一编译入口，区分观察、规则、历史、意图、分析和未知；整局战略、回合计划与具体动作使用不同生命周期。规则检索与实体关联共用类别/ID 注册表，显式暴露缺失项。旧模型评分留在本地，请求提供当前完整评分与所有发展方向变更，防止过期判断占满窗口。第二十三局已推进至第 13 层，四类实际决策已带入战略；同一局面的上下文由 70,550 降至 61,260 字节，详见[实机证据](docs/evidence/2026-09-20/run23-context-architecture.json)。通关仍未完成。

1. 验证回合决策管线：版本化本地规则库、完整实时状态、回合目标与顺序、资源预留、条件变化后保留或修改剩余计划。升级与普通出牌必须遵循同一受益对象意图。实际执行前后核对，未知效果保持未知。
2. 区分规则缺失、计算缺陷、上下文组织和模型判断问题。六个真实局面的初步回放已完成，尚无整局胜率改善证据；详细记录见[决策管线](docs/decision-pipeline.md)。
3. 只使用玩家能查看、观察和记住的信息。隐藏抽牌顺序、RNG、未来随机结果保持未知。不编辑存档，不作弊，不重置战斗，也不拼接不同局的成功片段。
4. 游戏内模组需要更新时，优先等正式失败退出后的主菜单再安装；打开地图不保证已保存完成的事件，避免部署造成房间回退。策略选择始终交给 Jev；唯一合法动作和纯流程推进由代码执行。
5. 按项目约定直接在 `master` 开发、commit 并 push，不创建本项目 PR。CLI 模组的实际缺陷可单独修复并向上游贡献；已有[兼容修复 Draft PR](https://github.com/longkerdandy/STS2-Cli-Mod/pull/1)。
6. 第一幕 Boss 录制与发送已完成：第十九局 Vantom 的完整录像保留本地；按用户最后要求，仅剪出击杀附近最后约 30 秒，通过 Computer Use 上传飞书本人 liushiao（刘世傲）会话，并经 OpenLark 3333 读取确认消息已同步。只录游戏窗口，720p/20fps、GPU 编码，无音轨。
7. 第十九局曾在第二幕第 19 层、57 生命因 TypeSafe 直连 `402 billing_error` 停在指令之前。按用户授权，已接入 OpenRouter 原生 Decisions API 和 `typesafe/jev-1.13`，沿用原有完整上下文与回合决策管线。真实局面完整规划的 19 个请求均成功，最大请求 68,092 字节；随后从同一局继续主循环，击败该场剩余敌人并进入奖励，战后 63 生命。凭据仅保存在忽略提交的本地配置。26 项相关检查及三种原生答案的实际调用通过；尚未完成三幕通关。
8. 已处理第 21 层选牌容量阻塞：共享状态与候选的重复说明、对选牌列表和地图使用可逆表格，并用专用选牌说明继承回合意图、区分生效时机。31 项完整请求从 74,631 降至 66,426 字节，实际成功后继续同一战斗；原计划的下一张 Anger 沿用顺序执行，无新模型请求。50 项相关检查通过。仍接近模型窗口，后续长战斗继续观察，超限保持在动作前停止。

## 完成条件

第二十局最终在第一幕第 13 层精英战正式失败，已归档并正常结算。已实机验证 Armaments 指定升级对象、选牌与后续出牌使用同一实例；整套方案比较统一战术说明，逐步展示预留能量并允许比较删除多余动作。地图增加每个分支的一条最少已知精英完整路线示例，第 9 层只读回放改选可避开已知精英的分支。149 项 Node 检查通过；这仍不是通关或胜率提升的证明，下一局继续实际验证。

第十九局最终在第二幕第 24 层 The Obscura 正式失败，分数 413，Boss 剩 79 生命。已保存完整记忆并正常结算。发现 Expect a Fight 的 25 点格挡因原生 CalculatedBlock 字段缺失被旧算术当作零，已修复通用提取，并为明确的实时首句提供回退；无法确定的格挡保留 unknown。失败前局面的只读回放改选 Expect a Fight，条件估算掉血从 18 降至 3，但仍遗漏免费攻击，不代表已经解决决策质量或提高整局胜率。

完整状态现以紧凑 JSON 字符串发送给原生 Decisions API，保留全部协议字段、规则与历史，日志保存实际 wire payload。原来超限的同一请求成功返回 22,856 输入 tokens；另外一个请求为 21,504。90 KB 门槛只约束转义前的本地逻辑内容，服务端仍独立执行 token 限制。回合规划新增内容复用已有字典，避免重复展开整套状态。

- [x] 完整决策上下文真正接入主循环，并完成必要构建与测试。
- [x] 普通奖励后继续游戏，移除原型的一场战斗停止限制。
- [ ] 同一局从第一幕经过三幕，击败最终 Boss，进入正式胜利结算。
- [ ] 在主分支提交最终实现，并同步实际运行结果和重要限制。
- [ ] 更新[现有飞书方案文档](https://icnainlav1b8.feishu.cn/docx/Ijr1dLJpio6JvNxfcAfcnvXKnUR)，展示最终胜利截图后，才结束 goal。

飞书正文已同步至修订 62，包含 OpenRouter、紧凑 JSON、context.16、第二十局正式失败结果、抽牌顺序与完整路线示例，并已回读确认。第二十一局已从正常新局流程开始。原架构画板表达总体状态—选择—执行通道。最终通关结果仍待完成。

## 运行与资料

- `npm start -- --max-steps 3000`：持续执行当前存档或正常新局。
- `node --use-system-ca scripts/run_recorded.mjs <ffmpeg.exe>`：运行同一主循环，并为下一次第一幕 Boss 自动录制；需要带 gfxcapture 和 h264_nvenc 的 FFmpeg。详见[录制说明](docs/boss-recording.md)。
- `npm run context:preview`：只读查看当前 JSON，不调用 Jev、不发送游戏动作、不修改记忆。
- `npm test`：运行离线回归测试。
- [决策上下文与 JSON 协议](docs/decision-context.md)、[Schema](schemas/decision-context.v1.schema.json)、[合成示例](docs/examples/decision-context.v1.json)。
- [架构图](docs/agent-loop.png)、[技术选型与早期资源验证](docs/technology-selection.md)、[早期单场战斗记录](docs/validation-2026-09-20.md)。早期记录只证明当时的单场后台运行。

仓库：[Sskift/jev-sts2-agent](https://github.com/Sskift/jev-sts2-agent)。实际运行日志和完整记忆保存在本机 `run-artifacts/`，不将凭据、存档或编译 DLL 提交到仓库。
