TYPESAFE_API_KEY=
# Set JEV_PROVIDER=openrouter to use its native Decisions endpoint.
JEV_PROVIDER=typesafe
# Leave empty for the provider default (OpenRouter: typesafe/jev-1.13).
JEV_MODEL=
OPENROUTER_API_KEY=
# Claude Code settings are read from the local Claude configuration; do not commit them here.
