# 当前工作计划

更新：2026-09-20。三幕正式通关目标尚未完成。本轮有序依赖修复和历史回放已完成，达到可以开始一局受观察验证的工程条件；本轮没有启动新对局。结果与未解决的策略反例见[评估报告](sequence-evaluation.md)。

## 本轮范围

1. 用上一轮暴露的局面修复顺序依赖：能量支付、属性变化、升级后的费用与效果、当前卡牌身份以及未知结果的观察断点，共用一次有序计算。
2. 将持久资源消耗、格挡的有效期、已知回合末结算和倒计时放进方案上下文。保留原生规则和 Wiki 关联资料，减少按某张牌追加战术提示。
3. 第 20、21、22、23、25、27 局用于开发；第 19、24、26 局按预定规则选第 2、4 回合做本轮留出比较，详见[划分](../eval/sequence-split.json)。这些历史局已有开发接触，不声称完全独立。
4. 冻结代码后，以同一历史输入和同一 Jev 模型对比旧、新实现，各重复两次。保留失败、中间版本与全部原始响应，按机制评估，不把方案变化或置信度当作胜率证据。
5. 达到恢复实战条件后进入下一阶段。保持 Node.js + C# 主路径，在 `master` 提交并推送，不为本仓库开 PR。

## 边界

正常循环通过 CLI 模组读取并执行，窗口无需置顶。规则库、相关历史和条件分析支持 Jev 在代码提供的候选空间内判断；它还不是完整的多回合求解器。未知抽牌、未揭示随机结果及尚未执行的效果保持未知。

上一轮的留出结果现在作为回归案例。本轮先统一能量、属性、升级后的手牌和观察断点，避免后续动作沿用失效预览或不存在的卡牌；再按[本轮划分](../eval/sequence-split.json)冻结和评估。未知结果需要观察，不能靠假定未来抽牌补全计划。

进入实战的依据是：已知机械错误修复、有序前缀可执行、重复回放没有继续出现同样的明显依赖错误。留出阶段发现的回收选牌断点已另行修复并复核；原冻结结果保留，不能声称最终版本未经留出反馈。模型对复活小怪的目标选择仍有反例，置信度和检查数量不单独作为依据，也不保证通关。录屏与发送任务此前已完成，不重复执行。

[README](../README.md) · [上下文架构](context-architecture.md) · [历史战况](full-run-progress.md)
