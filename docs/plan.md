# 当前工作计划

更新：2026-09-20。三幕正式通关目标尚未完成，已按用户要求暂停新对局。当前工作只使用保存的历史状态，修正上下文并比较 Jev 的决策，不向游戏发送命令。

## 本轮范围

1. 第 23—27 局作为开发资料；第 20、21、22 局作为本轮留出集。选取规则、重复次数和限制见[数据划分](../eval/harness-split.json)。这些历史局曾被早期开发分析，不声称完全未见过。
2. 对照本地 Spire Codex v0.111.0 和对应原生机制，明确反伤、临时增益、回合末结算的来源、触发、失效及未知范围。当前原生值优先，Wiki 示例数值不作为当前层数。
3. 冻结代码后，将相同历史观察和此前已确认的记忆分别交给旧、新 harness 与同一 Jev 模型。保留全部请求、原始响应和重复结果，不输入未来动作或结局。
4. 按机制判断改善、未变及退步，不把方案不同或模型置信度当作胜率证据。结果见[本轮报告](harness-evaluation.md)。
5. 保持 Node.js + C# 主路径；清除废弃视觉决策原型及演示入口，维护仓库内说明。在 `master` 提交并推送。

## 边界

正常循环通过 CLI 模组读取并执行，窗口无需置顶。规则库、相关历史和条件分析支持 Jev 在代码提供的候选空间内判断；它还不是完整的多回合求解器。未知抽牌、未揭示随机结果及尚未执行的效果保持未知。

本轮结束后保留实战暂停状态。留出集发现的问题先写入报告；如以后继续修改，应使用新的评估样本，避免反复用同一测试集调参。录屏与发送任务此前已完成，不重复执行。

[README](../README.md) · [上下文架构](context-architecture.md) · [历史战况](full-run-progress.md)
