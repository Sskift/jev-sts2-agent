# 当前工作计划

更新：2026-09-21。目标仍是从同一局标准新游戏的第一层开始，经过三幕、击败最终 Boss，并进入正式胜利结算。**尚未完成。** 官方接口阻塞后，现按用户要求改用 OpenRouter，并控制新增费用。

## 当前状态

第 36 局 `run-1789992163` 已加载并运行 `46e0b10`，OpenRouter Jev，规划候选评分上限 24。恢复最小化窗口后，第 35 局的失败结算按钮正常启用，经原生返回菜单，确认无存档后开始标准新局，铁甲战士、进阶 0、第一层；[启动证据](evidence/2026-09-21/run36-start.json)保留完整来源。累计 36 局启动、35 局结束、22 局通过第一幕，本局尚在推进。

第 35 局 `run-1789968641` 已结束，第二幕第 33 层 The Insatiable 第 7 回合失败，分数 644。累计 35 局启动并结束、22 局通过第一幕。死亡前 80 生命、43 格挡，Sandpit 为 1；最终手里没有 Frantic Escape，六张在未知顺序的抽牌堆中。原生 GAME_OVER 确认失败，pending=null；[复盘与证据](run35-review.md)已保存。goal 尚未完成。

第 34 局在第二幕第 21 层 The Obscura 第 11 回合正式失败；[复盘](run34-review.md)与[结算](evidence/2026-09-21/run34-review.json)已保存。确定性变形的作用范围修正与[回放证据](evidence/2026-09-21/run34-transformation.json)已提交推送，再启动本局；未回退或重打上一局战斗。

两把 TypeSafe 官方 key 曾返回计费错误，goal 因同一阻塞持续三轮被标记 blocked；工具不能将该状态改回 active，实际工作已按用户后续指令恢复。当前使用 OpenRouter `typesafe/jev-1.13`，回退关闭，付费候选评分上限 24。用户现已明确授权继续使用账户剩余余额，同时节省调用以收集更多对局。沿用同一持久费用账目、保留实际费用和未报告费用预留，余额查询与凭据仅留本机。旧 $1 停机记录是历史快照；不得将其当作仍待答复的授权或当前恢复点。

## 本轮已落实的修正

- [第 36 局的确定性续接](evidence/2026-09-21/run36-continuation.json)：根据原生代码补齐 Tremble 的易伤适配，只解释 Tuning Fork 尚未触发时的一次技能计数变化；触发动画、额外变化和新牌仍重新检查。45 个真实出牌快照中 6 个不必要的失效点被消除，5 个后续出牌点零新增调用续接；37 项相关检查通过。在第 15 层第 4 回合、46/80、1 能量保存了确认加载点，pending=null，提交推送后同局加载，不重打。

- [商店删牌与预算比较](shop-removal.md)：具体删牌目标、逐项购买后的余额与删牌机会、已有副本、保留牌的抽取示例；原生确认后续接同一目标。使用第 35 局三个开发快照，旧版／初版／最终版结果及一次直接比较全部保留，总费用 $0.017820474。关键 107 金局面在买牌与删牌间波动，双向比较也有分歧，不能视为确定改进或通关证明。

- [显示计数与游戏计数分离](evidence/2026-09-21/run35-relic-counter.json)：第 28 层与 Boss 开场因 Happy Flower 显示 3→0 分别作废 31／29 次响应。短暂等待的中间修正未可靠解决此问题，现已移除。原生 `TurnsSeen` 已按模 3 归零，独立动画 `DisplayAmount` 暂显 3；上下文、执行指纹、计划续接和新记忆共用这一事实语义，原始快照不改。71 项检查通过，三个真实前后快照的完整请求及执行核对均一致，其他真实变化仍失效；没有付费回放，最终修正尚未加载实战。
- 付费候选评分上限：保留 Jev 原计划、结束回合和同组牌换序，对其他资源组合及顺序做确定性采样；完整观察、规则、种子规划合法动作和双向最终比较不删减。每轮记录生成数、实评数及是否截断。43 项相关检查通过。此项减少搜索范围，可能漏选更好方案，不声称质量等价。
- 批次尺寸现在按无损压缩后的方案计算，避免可读展开使每个方案重复发送完整观察。在第 22 层第 5 回合的同一离线局面、同样回答下，34 个问题从 19 次请求／1,589,507 字节变为 11 次／978,616 字节，选择相同；49 项相关检查通过。这是传输验证，不是实际费用或胜率的对照实验。此前 OpenRouter 超时发生在游戏命令发送前，已确认 pending=null、原生指纹一致，恢复快照为 `temp/run35-before-openrouter-retry.json`；本次费用账目继续累计，不重置。
- [条件式回合末与敌方反应](evidence/2026-09-21/run35-end-reaction.json)：统一时序清单和结束估算对“If/When/Whenever you end … turn”的识别，关联延迟敌方伤害、阈值／阶段／死亡反应及剩余行动。原生确认 Parrying Shield 和 Plow 的钩子；真实快照的旧预测 42、实际 49，最终上下文改为未知，保留显示攻击 28 的基准。38 项检查和完整本地编译通过；原始失败调用与最终代码冻结均保留。官方 402 导致未取得新模型判断，不声称回放改善或完成了连锁模拟。
- [Slow 有序增幅](evidence/2026-09-21/run35-slow.json)：原生显示量是百分比，每张牌结算后增加 10 个百分点，下一张攻击才受益；已有增幅计入起始预览。现接入共用的有序伤害范围、属性变化和精确执行核对。60 项检查通过；两个独立历史续接点由 17／8 次新调用分别减为 0，保留各自原计划。完整官方回放仍选择先攻击后防御，没有利用已描述的增幅，6 对比较有 5 对位置分歧；保留这项未改善结果，不宣称策略提高。
- [有序变形对象](evidence/2026-09-21/run34-transformation.json)：按顺序列出剩余攻击、未受影响牌和关联的 Giant Rock 规则，升级依据是当时的 Primal Force，旧攻击升级不传递。32 项相关检查通过。第 5 回合回放由耗尽攻击再空变形，改为 Defend+ → Thunderclap → Primal Force，留下 Strike 与 1 能量。10 对比较仍有 2 对位置分歧；零攻击快照沿用历史计划、调用为 0，不算新的模型改善。没有游戏命令或反事实获胜结论。
- [已知能力变化后续接](evidence/2026-09-21/run34-debuff-continuation.json)：第二幕真实 Bash → Dismantle 原先在已预期的易伤之后仍重做 20 次调用。执行核对现复用共用的敌方条件账目，要求生命、格挡和能力层数精确一致，才保留原计划。相同前后快照零次模型调用续接原 Dismantle；34 项检查通过。意图、手牌和其他未知变化仍复核，不宣称提升胜率。
- 第 33 局最终失败的[复盘](run33-review.md)：在抽牌等观察节点保留已知攻击覆盖缺口，同时继续将未知抽牌和回合末结果标为未知。
- [目标相关命中次数](evidence/2026-09-21/run34-target-hits.json)：未解析的命中次数不再按零伤害处理。根据当前游戏原生规则核实 Dismantle 的易伤条件，将顺序变化、Artifact、伤害上限和反伤共用同一命中计数。54 项相关检查通过，已加载实战。
- [候选顺序覆盖](evidence/2026-09-21/run34-order-coverage.json)：旧回放只提供 Dismantle → Bash，遗漏 Bash → Dismantle 的大目标收尾。相邻换序现覆盖采样池与原提案，最多新增 96 个合法变体；同一顺序使用等价副本只评一次，保留目标、数量、属性和绑定关系。38 项相关检查通过。最终完整官方回放选择正确收尾，但 6 对比较仍有 4 对位置分歧，中间失败版本亦保留。

现有主流程仍为轻量 C# 原生模组 + Node.js。实时状态、Spire Codex v0.111.0 规则、五个角色策略和 115 个怪物行动图构成自包含上下文。仅保留仍影响决策的历史，不读取隐藏下一招、RNG 或未来抽牌。原生费用和目标预览优先于静态资料。

## 接下来的执行

1. 商店修正已提交推送并在第 36 局加载。重点观察实际删牌、预算取舍、重复加牌及关键牌可达性，同时记录计划执行与机制性死亡。只在重要差异上做低成本历史回放，保留未使用的留出局。
2. 如出现事实缺项、计算错误或候选遗漏，保存真实请求与原生结果，修复其通用原因。在已确认动作、pending=null、原生指纹匹配的位置加载；结果未知的游戏命令不自动重发，不重置战斗或回退存档。
3. 今后每局结束后先复盘：核对关键局面、完整候选、概率与置信度、实际执行和结算，区分上下文／计算问题、搜索范围、模型取舍与随机结果。有依据的调整提交推送后，才开始下一局。
4. 只有同一新局的原生进度经过三个幕索引 `[0,1,2]`，并取得 `GAME_OVER` 的 `is_victory=true`、`can_return_to_menu=true`，才将 goal 标为完成。

本仓库直接在 `master` 开发、commit 并 push，不提 PR。功能优先，验证覆盖实际改动即可。模组上游若需修复，再单独考虑贡献 PR。

## 已知边界

候选枚举、采样、相邻换序和最终入围均有上限。模型评分和双向比较仍可能漏选更好的方案；置信度与偏好支持不是已校准的正确率。当前没有完整跨回合战斗求解器，不能声称所有优化已穷尽，也不能将单例回放变化归因为确定的胜率提升。

[README](../README.md) · [决策管线](decision-pipeline.md) · [上下文架构](context-architecture.md) · [历次战况与阶段记录](full-run-progress.md)
